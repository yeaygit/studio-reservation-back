bind 0.0.0.0
protected-mode yes
port 6379

dir /data
appendonly yes
appendfsync everysec

save 60 1000
stop-writes-on-bgsave-error yes

timeout 0
tcp-keepalive 300

# JWT refresh tokens should not be evicted unexpectedly.
maxmemory-policy noeviction
